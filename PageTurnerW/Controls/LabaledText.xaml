<UserControl x:Class="PageTurnerW.Controls.LabeledText"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PageTurnerW.Controls"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="240">
    <Grid>
        <UserControl>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
        
                <TextBlock Grid.Column="0" Text="{Binding Label}" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Grid.Column="1" Text="{Binding Text}" Margin="0,0,0,0"/>
            </Grid>
        </UserControl>
    </Grid>
</UserControl>
