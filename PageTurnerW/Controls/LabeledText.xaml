<UserControl x:Class="PageTurnerW.Controls.LabeledText"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PageTurnerW.Controls"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="240"
             x:Name="Root">
    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Content="{Binding Label, ElementName=Root}" FontWeight="Bold" Margin="0"/>
        <TextBlock Grid.Column="1" Text="{Binding Text, ElementName=Root}" Margin="0"/>
    </Grid>
</UserControl>
